pkgname=dwmstb
pkgver=r10.0e93129
pkgrel=1
pkgdesc="Bog standard status bar for dwm"
url="https://github.com/fryedbiscuit/dwmstb"
arch=('i686' 'x86_64' 'arm' 'armv7h' 'armv6h' 'aarch64')
depends=('libx11' 'termux-api')
makedepends=('c3c' 'git' 'make')
source=(git+https://github.com/fryedbiscuit/dwmstb.git#branch=main)
sha256sums=('SKIP')

pkgver() {
	printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

build() {
	cd dwmstb
	c3c build termux
}

package() {
	install -d ${pkgdir}${PREFIX}/bin
	install -m 755 dwmstb/build/dwmstb-termux ${pkgdir}${PREFIX}/bin/dwmstb
}
